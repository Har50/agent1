---
title: Claude & Cursor Integration Guide
description: Point MCP clients at the AgentExec gateway.
---

1. Run API + gateway (`npm run prod:up` or `npm run dev`)
2. Install MCP server package / use `npm run mcp`
3. Add MCP config (see [Quickstart](/docs/getting-started/quickstart-mcp))
4. Prefer `EXECUTION_MODE=simulate` until Tenderly + paymaster are funded

Repo reference: `docs/MCP.md` and `docs/claude_desktop_config.example.json`.
